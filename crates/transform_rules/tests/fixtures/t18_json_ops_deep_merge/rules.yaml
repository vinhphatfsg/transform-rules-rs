version: 1
input:
  format: json
  json: {}
mappings:
  - target: "merged"
    expr:
      op: "deep_merge"
      args:
        - { ref: "input.base" }
        - { ref: "input.override" }
        - { ref: "input.extra" }
